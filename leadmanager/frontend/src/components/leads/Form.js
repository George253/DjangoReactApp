import React, { PureComponent } from 'react'

export default class Form extends PureComponent {
    render() {
        return (
            <div>
                <h1>Add Form</h1>
            </div>
        )
    }
}
